# ═══════════════════════════════════════════════════════════════
# VARIABLES DE ENTORNO - PLATAFORMA DE AUTONOMÍA FEMENINA
# ═══════════════════════════════════════════════════════════════
# Este archivo contiene las configuraciones necesarias para el
# funcionamiento del Custom Server y la integración con Supabase
# ═══════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────
# CONFIGURACIÓN DEL SERVIDOR
# ───────────────────────────────────────────────────────────────

# Entorno de ejecución (development | production)
NODE_ENV=development

# Hostname del servidor
HOSTNAME=localhost

# Puerto del servidor (HTTP + WebSockets)
PORT=3000

# URL del frontend (para CORS en producción)
FRONTEND_URL=http://localhost:3000

# ───────────────────────────────────────────────────────────────
# CONFIGURACIÓN DE SUPABASE
# ───────────────────────────────────────────────────────────────

# URL de tu proyecto de Supabase
# Formato: https://tu-proyecto.supabase.co
# Obtener en: Dashboard de Supabase > Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://tu-proyecto.supabase.co

# Clave pública (anon key) de Supabase
# Esta clave es segura para usar en el frontend
# Obtener en: Dashboard de Supabase > Settings > API > Project API keys > anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=tu-clave-publica-aqui

# Clave de servicio (service_role key) de Supabase
# ⚠️  IMPORTANTE: Esta clave NUNCA debe exponerse en el frontend
# Solo usarla en el backend (server.js, chatService.js)
# Obtener en: Dashboard de Supabase > Settings > API > Project API keys > service_role
SUPABASE_SERVICE_ROLE_KEY=tu-clave-de-servicio-aqui

# ───────────────────────────────────────────────────────────────
# CONFIGURACIÓN DE SOCKET.IO (OPCIONAL)
# ───────────────────────────────────────────────────────────────

# Timeout de ping en milisegundos (default: 60000)
SOCKET_PING_TIMEOUT=60000

# Intervalo de ping en milisegundos (default: 25000)
SOCKET_PING_INTERVAL=25000

# ───────────────────────────────────────────────────────────────
# CONFIGURACIÓN DE AUTENTICACIÓN (OPCIONAL)
# ───────────────────────────────────────────────────────────────

# JWT Secret (si usas autenticación personalizada)
# JWT_SECRET=tu-secreto-jwt-ultra-seguro-aqui

# ═══════════════════════════════════════════════════════════════
# NOTAS DE CONFIGURACIÓN
# ═══════════════════════════════════════════════════════════════
#
# 1. CÓMO OBTENER LAS CREDENCIALES DE SUPABASE:
#    - Ve a https://supabase.com/dashboard
#    - Selecciona tu proyecto
#    - Ve a Settings > API
#    - Copia la URL del proyecto y las API keys
#
# 2. SEGURIDAD:
#    - NUNCA subas este archivo a Git (ya está en .gitignore)
#    - La clave service_role tiene acceso completo a la BD
#    - Solo usar service_role en backend, nunca en frontend
#
# 3. PRODUCCIÓN:
#    - Configura estas variables en tu plataforma de hosting
#    - Vercel: Project Settings > Environment Variables
#    - Railway: Variables tab
#    - Docker: Archivo .env o docker-compose.yml
#
# 4. DESARROLLO EN EQUIPO:
#    - Comparte las credenciales de forma segura (1Password, etc.)
#    - Cada desarrollador debe tener su propio .env.local
#    - Opción: Usar un proyecto de Supabase compartido para desarrollo
#
# ═══════════════════════════════════════════════════════════════
